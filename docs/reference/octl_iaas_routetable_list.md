## octl iaas routetable list

alias for api ReadRouteTables

### Synopsis

> *alias for api ReadRouteTables*

Lists one or more of your route tables.

In your Net, each Subnet must be associated with a route table. If a Subnet is not explicitly associated with a route table, it is implicitly associated with the main route table of the Net.

```
octl iaas routetable list [flags]
```

### Options

```
  -h, --help                                           help for list
      --id strings                                     The IDs of the route tables.
      --link-route-table-id strings                    The IDs of the route tables involved in the associations.
      --link-route-table-link-route-table-id strings   The IDs of the associations between the route tables and the Subnets.
      --link-route-table-main                          If true, the route tables are the main ones for their Nets.
      --link-subnet-id strings                         The IDs of the Subnets involved in the associations.
      --net-id strings                                 The IDs of the Nets for the route tables.
      --route-creation-method strings                  The methods used to create a route.
      --route-destination-ip-range strings             The IP ranges specified in routes in the tables.
      --route-destination-service-id strings           The service IDs specified in routes in the tables.
      --route-gateway-id strings                       The IDs of the gateways specified in routes in the tables.
      --route-nat-service-id strings                   The IDs of the NAT services specified in routes in the tables.
      --route-net-peering-id strings                   The IDs of the Net peerings specified in routes in the tables.
      --route-state strings                            The states of routes in the route tables (always active).
      --route-vm-id strings                            The IDs of the VMs specified in routes in the tables.
      --tag strings                                    The key/value combination of the tags associated with the route tables, in the following format: "Filters":{"Tags":["TAGKEY=TAGVALUE"]}.
      --tag-key strings                                The keys of the tags associated with the route tables.
      --tag-value strings                              The values of the tags associated with the route tables.
```

### Options inherited from parent commands

```
  -c, --columns string    columns to display - [+]<title>:<jq query for content>||<title>:<jq query for content>
      --config string     Path of profile file (by default, ~/.osc/config.json)
      --filter strings    comma separated list of filters for results - name:value,name:value, alias for jq filter 'select(.name | test("value"))'
      --jq string         jq filter
      --no-upgrade        do not check for new versions
  -O, --out-file string   redirect output to file
  -o, --output string     output format (raw, json, yaml, table, csv, none, base64) (default "raw")
      --profile string    Profile to use in profile file (by default, "default")
      --single            convert single entry lists to a single object
      --template string   JSON template for query body
  -v, --verbose           Verbose output
  -y, --yes               answer yes to all prompts
```

### SEE ALSO

* [octl iaas routetable](octl_iaas_routetable.md)	 - routetable commands

###### Auto generated by spf13/cobra on 6-Mar-2026
