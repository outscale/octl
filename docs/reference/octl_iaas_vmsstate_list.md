## octl iaas vmsstate list

alias for api ReadVmsState

### Synopsis

> *alias for api ReadVmsState*

Lists the status of one or more virtual machines (VMs).

```
octl iaas vmsstate list [flags]
```

### Options

```
      --all-vm                                  If true, includes the status of all VMs.
  -h, --help                                    help for list
      --maintenance-event-code strings          The code for the scheduled event (system-reboot | system-maintenance).
      --maintenance-event-description strings   The description of the scheduled event.
      --maintenance-event-not-after strings     The latest date and time (UTC) the event can end.
      --maintenance-event-not-before strings    The earliest date and time (UTC) the event can start.
      --subregion strings                       The names of the Subregions of the VMs.
      --vm-id strings                           One or more IDs of VMs.
      --vm-state strings                        The states of the VMs (pending | running | stopping | stopped | shutting-down | terminated | quarantine).
```

### Options inherited from parent commands

```
  -c, --columns string    columns to display - [+]<title>:<jq query for content>||<title>:<jq query for content>
      --config string     Path of profile file (by default, ~/.osc/config.json)
      --filter strings    comma separated list of filters for results - name:value,name:value, alias for jq filter 'select(.name | test("value"))'
      --jq string         jq filter
      --no-upgrade        do not check for new versions
  -O, --out-file string   redirect output to file
  -o, --output string     output format (raw, json, yaml, table, csv, none, base64) (default "raw")
      --profile string    Profile to use in profile file (by default, "default")
      --single            convert single entry lists to a single object
      --template string   JSON template for query body
  -v, --verbose           Verbose output
  -y, --yes               answer yes to all prompts
```

### SEE ALSO

* [octl iaas vmsstate](octl_iaas_vmsstate.md)	 - vmsstate commands

###### Auto generated by spf13/cobra on 6-Mar-2026
