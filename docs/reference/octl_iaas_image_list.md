## octl iaas image list

alias for api ReadImages

### Synopsis

> *alias for api ReadImages*

Lists one or more OUTSCALE machine images (OMIs) you can use.

```
octl iaas image list [flags]
```

### Options

```
      --account-alias strings                     The account aliases of the owners of the OMIs.
      --account-id strings                        The account IDs of the owners of the OMIs.
      --architecture strings                      The architectures of the OMIs (i386 | x86_64).
      --boot-mode strings                         The boot modes compatible with the OMIs.
      --description strings                       The descriptions of the OMIs, provided when they were created.
      --file-location strings                     The locations of the buckets where the OMI files are stored.
  -h, --help                                      help for list
      --hypervisor strings                        The hypervisor type of the OMI (always xen).
      --id strings                                The IDs of the OMIs.
      --name strings                              The names of the OMIs, provided when they were created.
      --permission-to-launch-account-id strings   The account IDs which have launch permissions for the OMIs.
      --permission-to-launch-global-permission    If true, lists all public OMIs.
      --product-code strings                      The product codes associated with the OMI.
      --product-code-name strings                 The names of the product codes associated with the OMI.
      --root-device-name strings                  The name of the root device.
      --root-device-type strings                  The types of root device used by the OMIs (bsu or ebs).
      --secure-boot                               Whether secure boot is activated or not.
      --state strings                             The states of the OMIs (pending | available | failed).
      --tag strings                               The key/value combination of the tags associated with the OMIs, in the following format: "Filters":{"Tags":["TAGKEY=TAGVALUE"]}.
      --tag-key strings                           The keys of the tags associated with the OMIs.
      --tag-value strings                         The values of the tags associated with the OMIs.
      --tpm-mandatory                             Whether a virtual Trusted Platform Module (vTPM) is mandatory for VMs created from this OMI (true) or not (false).
      --virtualization-type strings               The virtualization types (always hvm).
      --volume-delete-on-vm-deletion              Whether the volumes are deleted or not when terminating the VM.
      --volume-device-name strings                The device names for the volumes.
      --volume-size ints                          The sizes of the volumes, in gibibytes (GiB).
      --volume-snapshot-id strings                The IDs of the snapshots used to create the volumes.
      --volume-type strings                       The types of volumes (standard | gp2 | io1).
```

### Options inherited from parent commands

```
  -c, --columns string    columns to display - [+]<title>:<jq query for content>||<title>:<jq query for content>
      --config string     Path of profile file (by default, ~/.osc/config.json)
      --filter strings    comma separated list of filters for results - name:value,name:value, alias for jq filter 'select(.name | test("value"))'
      --jq string         jq filter
      --no-upgrade        do not check for new versions
  -O, --out-file string   redirect output to file
  -o, --output string     output format (raw, json, yaml, table, csv, none, base64) (default "raw")
      --profile string    Profile to use in profile file (by default, "default")
      --single            convert single entry lists to a single object
      --template string   JSON template for query body
  -v, --verbose           Verbose output
  -y, --yes               answer yes to all prompts
```

### SEE ALSO

* [octl iaas image](octl_iaas_image.md)	 - image commands

###### Auto generated by spf13/cobra on 6-Mar-2026
